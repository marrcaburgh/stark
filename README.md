# cli_opts
Command line parser for C/C++

# Build

1. clone the repo `git clone https://github.com/marrcaburgh/cli_opts/cli_opts.git`
  
3. run `cmake --preset release-no-tests`
  
5. run `cmake --build --preset release-no-tests`
